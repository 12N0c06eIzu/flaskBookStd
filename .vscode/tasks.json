{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "echo",
            "type": "shell",
            "command": "pwd"
        },
        {
            "label": "runserver minimalapp",
            "type": "shell",
            "command": "pipenv run flask run",
            "args": [
                ""
            ],
            "options": {
                "cwd": "${workspaceFolder}/flaskBookApp/apps/minimalapp"
            }
        }
    ]
}
